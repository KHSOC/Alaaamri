# Cloudflare Pages serves courses.html automatically at /courses.
# Do not proxy /courses back to courses.html because Pages redirects
# .html URLs to their extension-less equivalents, which creates a loop.
